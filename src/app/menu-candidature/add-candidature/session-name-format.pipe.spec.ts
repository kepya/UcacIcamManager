import { SessionNameFormatPipe } from './session-name-format.pipe';

describe('SessionNameFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new SessionNameFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
